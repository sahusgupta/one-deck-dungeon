gameId - game:
  active: boolean
  playerList - Array<Player>:
    id: string
    skills: Array<string>
    items:
      values: Array<number>
    defeatedEncounters - Array<[Encounter, boolean]>: Array<string-boolean>
    hero: string
  dungeon - Dungeon:
    name: string
  potions: number
  level: number
  playerNum: number
  chatLog: Array<string>
  activeEncounterRuntime - EncounterRuntime | undefined:
    encounter: string
    workspaceIndex: number
    availableDice: Array<number|number|number-boolean> # uses IDs
    diceInBox: Array<number|number|number-number-number|number|boolean|number|number|number>
    necessaryDice: Array<number|number|boolean|number|number|number>
  workspace - Array<[Encounter, boolean]>: Array<string-boolean>
  deck: Array<string>
  discard: Array<string>
# gonna have to make sure that when re-initializing 
# dice-in-box and necessary diceboxes make sure to keep
# referencing the same